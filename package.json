{
  "name": "hakimos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "HakimOS — a complete desktop operating system that runs in your browser",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
